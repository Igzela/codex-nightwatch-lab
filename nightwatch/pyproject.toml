[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "nightwatch"
version = "0.5.4"
description = "Linux-first, fail-closed unattended supervisor (Codex, AGY)"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools]
packages = ["nightwatch"]

[tool.setuptools.package-data]
nightwatch = ["nightwatch.service"]

[project.scripts]
nightwatch = "nightwatch.cli:main"
